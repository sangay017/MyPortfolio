{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "trailingSlash": false,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "${VITE_BACKEND_URL}/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
