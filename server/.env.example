# Server environment example

# MongoDB connection string
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority

# JWT settings
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRE=30d

# Allowed frontend origin(s) for CORS
# - Comma-separated list supported
# - Wildcards supported, e.g. https://*.vercel.app
# - Trailing slashes are ignored
# Examples:
# FRONTEND_URL=https://your-frontend.vercel.app
# FRONTEND_URL=https://your-frontend.vercel.app,https://localhost:5173
# FRONTEND_URL=https://*.vercel.app
FRONTEND_URL=http://localhost:5173

# Email (Brevo SMTP) for contact form
BREVO_SMTP_HOST=smtp-relay.brevo.com
BREVO_SMTP_PORT=587
BREVO_SMTP_USER=<brevo-login-email-or-username>
BREVO_SMTP_PASS=<brevo-smtp-api-key>
FROM_EMAIL=you@example.com
FROM_NAME=Portfolio Contact Form
TO_EMAIL=you@example.com
BREVO_BCC_SELF=false
BREVO_SMTP_DEBUG=false

# OpenAI / Router for Chatbot
# If using OpenRouter, set OPENAI_BASE_URL=https://openrouter.ai/api/v1 and OPENAI_MODEL accordingly
OPENAI_API_KEY=
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=qwen/qwen2.5-vl-32b-instruct:free

# Local dev port (not used on Vercel)
PORT=5000
